// ----------------------------------------------------------------
// PURPOSE:
// This library implements the Enum for supported alignments.
// ----------------------------------------------------------------

export enum Alignment {
    CENTER = 'center',
    JUSTIFIED = 'justify',
    LEFT = 'left',
    RIGHT = 'right',
}

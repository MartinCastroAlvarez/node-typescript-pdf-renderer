// ----------------------------------------------------------------
// PURPOSE:
// This library implements the Enum for supported syntaxes.
// ----------------------------------------------------------------

export enum Syntax {
    PYTHON = 'py',
    NODE= 'js',
}

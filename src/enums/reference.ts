// ----------------------------------------------------------------
// Purpose:
// This library implements the Enum for supported References.
// ----------------------------------------------------------------

export enum Reference {
    FONTS = '@fonts',
    IMAGES = '@image',
    BOOKS = '@books',
    CONFIG = '@config',
    PERSONS = '@person',
    TOPICS = '@topic',
    FILES = '@file',
}

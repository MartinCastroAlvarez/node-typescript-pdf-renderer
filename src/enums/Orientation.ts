// ----------------------------------------------------------------
// PURPOSE:
// This library implements the Enum for supported orientations.
// ----------------------------------------------------------------

export enum Orientation {
    LANDSCAPE = 'landscape',
    PORTRAIT = 'portrait',
    SQUARE = 'square',
}

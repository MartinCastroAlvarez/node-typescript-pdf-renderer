// ----------------------------------------------------------------
// Purpose:
// This library implements the Format enum.
// ----------------------------------------------------------------

export enum Format {
    PDF = 'pdf',
    COURSE = 'course',
    HTML = 'html',
    VIDEO = 'video',
}

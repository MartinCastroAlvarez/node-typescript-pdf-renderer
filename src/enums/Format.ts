// ----------------------------------------------------------------
// PURPOSE:
// This library implements the Format enum.
// ----------------------------------------------------------------

export enum Format {
    EBOOK = 'ebook',
    COURSE = 'course',
    HTML = 'html',
    VIDEO = 'video',
}
